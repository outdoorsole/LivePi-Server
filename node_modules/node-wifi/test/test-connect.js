var wifi = require('../src/wifi');

wifi.init({
    debug : true,
    iface : 'wlan0'
});

var ap = {
    ssid : "MakeStudent",
    password : "applynow"
}

wifi.connect(ap, function(err) {
    if (err) {
        console.log(err);
    } else {
        console.log('connected');
    }
});
